{
    "name": "job-application-tracker",
    "version": "2.0.0",
    "description": "Chrome extension for tracking job applications",
    "main": "background.js",
    "scripts": {
      "test": "jest",
      "build": "mkdir -p dist && cp manifest.json popup.html popup.js background.js dist/"
    },
    "devDependencies": {
      "jest": "^27.0.6",
      "jsdom": "^16.6.0",
      "sinon-chrome": "^3.0.1"
    }
  }